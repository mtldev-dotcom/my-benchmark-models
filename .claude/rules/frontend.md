# Frontend Rules

- Use Tailwind utility classes only (avoid ad-hoc CSS unless justified)
- Mobile-first layout and interactions
- Prefer reusable components over one-off duplication
- Keep UI minimal, scannable, and accessible
- Strong TypeScript types for props/state
- Avoid inline styles unless dynamic and unavoidable
- Keep loading/running/error states explicit in UI
